[audio]
identify latency issues
    run kokoro TTS locally (avoid network roundtrips) - moonshine
    true streaming (stream LLM and TTS on the fly per sentence)
    local LLM or hit deepseek API directly
switch to mqtt
    logger config to support all
    refine topics/channels, add more helpful stuff
shazamio integration
optimize system prompt
INMP441
finetune wake word model w/ more data in noisy environments, different voices, etc


[motor/camera]
switch to mqtt